import {RestServiceClient} from './RestServiceClient'

export class {{pascalCase name}}Service extends RestServiceClient {
  constructor (token = undefined) {
      super(BASE_URL, {authorizationToken: token})
      this.token = token
  }
}
